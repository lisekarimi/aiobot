# Keys for main application
OPENAI_API_KEY=
TICKETMASTER_KEY=

# LLM Model Configuration
MODEL=gpt-4o-mini

# Weather API key (optional - only used as fallback if Open-Meteo fails)
# Strategy: Uses FREE Open-Meteo by default, only uses paid API if needed
# Get free key from: https://www.weatherapi.com/signup.aspx
WEATHER_API_KEY=

# Usernames used for Makefile
DOCKER_USERNAME=
